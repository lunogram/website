---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class:list={[
  'grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-4 max-w-6xl mx-auto',
  className,
]}>
  <slot />
</div>

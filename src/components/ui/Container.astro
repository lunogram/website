---
import { clsx } from 'clsx';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={clsx('px-4 sm:px-6 lg:px-8', className)}>
  <div class="mx-auto max-w-2xl lg:max-w-7xl">
    <slot />
  </div>
</div>
